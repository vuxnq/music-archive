@model List<MusicArchive.Domain.Models.User>

@{
    ViewBag.Title = "Users";
    Layout = "_Layout";
}

<h2>Users</h2>

<ul>
    @foreach (var user in Model)
    {
        <li>
            @Html.ActionLink(user.Username, "Detail", new { id = user.Id })
        </li>
    }
</ul>
