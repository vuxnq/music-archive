@model List<MusicArchive.Domain.Models.Track>

@{
    ViewBag.Title = "Tracks";
    Layout = "_Layout";
}

<h2>@ViewBag.Title</h2>
@Html.ActionLink("Propose New Track", "Add", "Track")
<ul>
    @foreach (var track in Model) {
        <li>
            @Html.ActionLink(track.Title, "Detail", "Track", new { id = track.Id }, null)
        </li>
    }
</ul>
